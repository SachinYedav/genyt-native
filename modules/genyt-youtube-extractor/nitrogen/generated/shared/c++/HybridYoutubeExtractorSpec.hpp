///
/// HybridYoutubeExtractorSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NativePaginatedVideoResult` to properly resolve imports.
namespace margelo::nitro::genyt::extractor { struct NativePaginatedVideoResult; }
// Forward declaration of `NativeExtractionResult` to properly resolve imports.
namespace margelo::nitro::genyt::extractor { struct NativeExtractionResult; }
// Forward declaration of `NativeResolvedFormat` to properly resolve imports.
namespace margelo::nitro::genyt::extractor { struct NativeResolvedFormat; }
// Forward declaration of `NativeChannelDetails` to properly resolve imports.
namespace margelo::nitro::genyt::extractor { struct NativeChannelDetails; }
// Forward declaration of `NativePlaylistDetails` to properly resolve imports.
namespace margelo::nitro::genyt::extractor { struct NativePlaylistDetails; }

#include <string>
#include "NativePaginatedVideoResult.hpp"
#include <NitroModules/Promise.hpp>
#include <optional>
#include "NativeExtractionResult.hpp"
#include "NativeResolvedFormat.hpp"
#include "NativeChannelDetails.hpp"
#include "NativePlaylistDetails.hpp"

namespace margelo::nitro::genyt::extractor {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `YoutubeExtractor`
   * Inherit this class to create instances of `HybridYoutubeExtractorSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridYoutubeExtractor: public HybridYoutubeExtractorSpec {
   * public:
   *   HybridYoutubeExtractor(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridYoutubeExtractorSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridYoutubeExtractorSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridYoutubeExtractorSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::string healthCheck() = 0;
      virtual std::shared_ptr<Promise<NativePaginatedVideoResult>> search(const std::string& requestId, const std::string& query, double limit) = 0;
      virtual std::shared_ptr<Promise<NativePaginatedVideoResult>> getHomeFeed(const std::string& requestId, const std::optional<std::string>& topic, double limit) = 0;
      virtual std::shared_ptr<Promise<NativePaginatedVideoResult>> fetchNextPage(const std::string& requestId, const std::string& token) = 0;
      virtual std::shared_ptr<Promise<NativeExtractionResult>> extractVideo(const std::string& requestId, const std::string& videoIdOrUrl) = 0;
      virtual std::shared_ptr<Promise<NativeResolvedFormat>> resolveFormat(const std::string& requestId, const std::string& sessionId, const std::string& formatId) = 0;
      virtual std::shared_ptr<Promise<NativeChannelDetails>> getChannelDetails(const std::string& requestId, const std::string& channelIdOrUrl, const std::optional<std::string>& tabFilter) = 0;
      virtual std::shared_ptr<Promise<NativePlaylistDetails>> getPlaylistDetails(const std::string& requestId, const std::string& playlistIdOrUrl) = 0;
      virtual void cancel(const std::string& requestId) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "YoutubeExtractor";
  };

} // namespace margelo::nitro::genyt::extractor
