# Extraction Service

Keep source extraction behind a stable TypeScript interface. The first production
implementation can call a native module, while the UI continues to depend only on
the service contract.
