import { nativeSourceAdapter } from './nativeSourceAdapter';

export const sourceAdapter = nativeSourceAdapter;

export { feedTopics } from './topics';
export type { SourceAdapter, SourceFeedParams, SourceSearchParams } from './types';
